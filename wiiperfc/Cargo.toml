[package]
name = "wiiperfc"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
wiiperf = { path = "../wiiperf" }
wiistd = { path = "../wiistd", features = ["panic_handler"] }
